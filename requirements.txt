# Railway's Nixpacks builder reliably auto-installs from requirements.txt but
# doesn't pick up plain PEP 621 dependencies in pyproject.toml (no Poetry/PDM
# lock file here). This must be kept in sync with [project.dependencies] in
# pyproject.toml — that file is still the source of truth for local dev.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
jinja2>=3.1
pydantic[email]>=2.9
pydantic-settings>=2.6
passlib[argon2]>=1.7.4
itsdangerous>=2.2
python-multipart>=0.0.12
